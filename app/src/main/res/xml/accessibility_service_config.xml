<?xml version="1.0" encoding="utf-8"?>
<!--
  packageNames is deliberately restricted to the Settings app: the service can only
  read window content there, never in your other apps. If your OEM hosts "App info"
  in a different package, add it here and to ForceStopAccessibilityService.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:packageNames="com.android.settings"
    android:summary="@string/accessibility_service_summary" />
